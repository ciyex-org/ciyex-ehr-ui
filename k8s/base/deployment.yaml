apiVersion: apps/v1
kind: Deployment
metadata:
  name: ciyex-ehr-ui
  labels:
    app: ciyex-ehr-ui
    app.kubernetes.io/name: ciyex-ehr-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ciyex-ehr-ui
  template:
    metadata:
      labels:
        app: ciyex-ehr-ui
    spec:
      containers:
        - name: ciyex-ehr-ui
          image: nginx:alpine
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
